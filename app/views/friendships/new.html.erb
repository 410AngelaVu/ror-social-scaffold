<h1>Welcome Friend</h1>
<%= form_with model: @friendship, url: user_friendship_path do |f| %>
<div class="field">
	<%= f.label :friend_id %> <br>
	<%= f.number_field :friend_id, autofocus: true%>
	<%= f.submit 'Friends!' %>
</div>
	<% end %>